import{LOCATION_CHAINS,PACK_COUNTS,productName,parsePackage,retailerCode,retailerName,canonicalKey}from'../common.js';
const SEARCHES=['https://etilbudsavis.no/soek/coca%20cola%20zero','https://etilbudsavis.no/soek/pepsi%20max'];
async function page(url){const r=await fetch(url,{headers:{Accept:'text/html','User-Agent':'JaktenPaDetSorteGull/3.0 (+price-comparison)'}});if(!r.ok)throw Error(`eTilbudsavis ${r.status}`);return r.text()}
function extractData(html){const start=html.indexOf('{"data":[');if(start<0)return[];let depth=0,str=false,esc=false;for(let i=start;i<html.length;i++){const c=html[i];if(str){if(esc)esc=false;else if(c==='\\')esc=true;else if(c==='"')str=false;continue}if(c==='"'){str=true;continue}if(c==='{')depth++;else if(c==='}'){depth--;if(depth===0){try{return JSON.parse(html.slice(start,i+1)).data||[]}catch{return[]}}}}return[]}
export async function getOffers(location){const allowed=new Set(LOCATION_CHAINS[location]||[]);const docs=await Promise.all(SEARCHES.map(async u=>{try{return await page(u)}catch{return''}}));const now=Date.now(),rows=[];for(const item of docs.flatMap(extractData)){const text=`${item.name||''} ${item.description||''}`,product=productName(text),pack=parsePackage(text);if(!product||!pack)continue;if(pack.count>1&&!PACK_COUNTS.has(pack.count))continue;if(pack.count===1&&!['033','05','15'].includes(pack.sizeKey))continue;const chainCode=retailerCode(item.business?.name||'');if(!chainCode||!allowed.has(chainCode))continue;const from=Date.parse(item.validFrom),until=Date.parse(item.validUntil);if(!Number.isFinite(from)||!Number.isFinite(until)||now<from||now>until)continue;const raw=item.membershipPrice??item.appPrice??item.price,price=Number(raw);if(!Number.isFinite(price))continue;rows.push({product,...pack,chainCode,chain:retailerName(chainCode),price,offerPrice:price,regularPrice:null,offer:true,kind:'offer',priceType:item.membershipPrice!=null?'member':item.appPrice!=null?'app':'public',validFrom:item.validFrom,validUntil:item.validUntil,source:'eTilbudsavis',sourceId:item.publicId||null,sourceUrl:null})}const best=new Map();for(const x of rows){const k=canonicalKey(x),p=best.get(k);if(!p||x.price<p.price)best.set(k,x)}return{rows:[...best.values()],source:'eTilbudsavis'}}
