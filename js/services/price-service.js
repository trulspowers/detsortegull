const pid=n=>n==='Pepsi Max'?'pepsiMax':n==='Coca-Cola Zero'?'cokeZero':null;
function norm(p){const o={cokeZero:[],pepsiMax:[]};for(const g of p?.chains||[])for(const x of g.products||[]){const id=pid(x.product);if(id)o[id].push({store:g.chain,pack:x.pack,price:x.price,offer:x.kind==='offer',member:false,sizeKey:x.sizeKey||'other',logo:x.logo||null})}return o}
export async function getPrices(location){try{const r=await fetch(`/api/prices?location=${encodeURIComponent(location)}`),p=await r.json();if(!r.ok)throw Error(p.detail||String(r.status));if(p.source==='unavailable')return{data:{cokeZero:[],pepsiMax:[]},source:'unavailable',updatedAt:null,note:p.note};const data=norm(p);if(!data.cokeZero.length&&!data.pepsiMax.length)throw Error('Ingen relevante produkter funnet');return{data,source:p.source,updatedAt:p.updatedAt,note:'Kjedepriser for kjeder i valgt område'}}catch(e){return{data:{cokeZero:[],pepsiMax:[]},source:'error',updatedAt:null,note:e.message}}}