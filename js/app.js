import{locations}from'./data/locations.js';import{getState,setState}from'./state/store.js';import{getPrices}from'./services/price-service.js';const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];let priceData={cokeZero:[],pepsiMax:[]},priceMeta={source:'loading'},loadToken=0,sizeFilter='all';const volume=p=>{const m=p.match(/(?:(\d+)\s*×\s*)?([\d,.]+)\s*L/i);return m?+(m[1]||1)*+m[2].replace(',','.'):1},money=(n,c)=>new Intl.NumberFormat(c==='SEK'?'sv-SE':'nb-NO',{maximumFractionDigits:2}).format(n);function toast(a,b=''){const t=$('#toast');t.innerHTML=`<strong>${a}</strong>${b?`<small>${b}</small>`:''}`;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2500)}function status(){if(priceMeta.source==='loading')return'Oppdaterer priser…';if(priceMeta.source==='preview')return'Forhåndsvisningsdata · Sverige';if(priceMeta.source==='error')return`Prisinnhenting feilet · ${priceMeta.note||'prøv igjen'}`;const d=new Date(priceMeta.updatedAt);return`Kassalapp · oppdatert ${d.toLocaleString('nb-NO',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})} · kjedepriser`}
function render(){const s=getState(),loc=locations[s.location]||locations.sandefjord;$('#locationLabel').textContent=loc.name;$('#dataStatus').textContent=status();$('#dataStatus').dataset.source=priceMeta.source;$$('#locationMenu [data-location]').forEach(b=>b.classList.toggle('active',b.dataset.location===s.location));$$('.size-chip').forEach(b=>b.classList.toggle('active',b.dataset.size===sizeFilter));$$('.card').forEach(c=>{const id=c.dataset.product;c.hidden=s.productFilter!=='all'&&s.productFilter!==id;c.querySelector('.heart-btn')?.classList.toggle('active',s.favorites.includes(id));let rows=(priceData[id]||[]).map(x=>({...x,ppl:x.price/volume(x.pack)}));if(sizeFilter!=='all')rows=rows.filter(x=>x.sizeKey===sizeFilter);if(s.offerOnly)rows=rows.filter(x=>x.offer);if(s.memberOnly)rows=rows.filter(x=>x.member);rows.sort(s.sort==='total'?(a,b)=>a.price-b.price:s.sort==='store'?(a,b)=>a.store.localeCompare(b.store):(a,b)=>a.ppl-b.ppl);c.querySelector('.price-list').innerHTML=rows.length?rows.map(x=>`<div class="price-row"><div class="store-logo">${x.logo&&/^https?:/.test(x.logo)&&!x.logo.includes('localhost')?`<img src="${x.logo}" alt="${x.store}" onerror="this.replaceWith(document.createTextNode('${x.store.replace(/'/g,'')}'))">`:x.store}</div><div class="price-meta">${x.pack}<small>${money(x.ppl,loc.currency)} ${loc.currency}/L</small></div><div class="amount ${x.offer?'offer':''}">${money(x.price,loc.currency)} <small>${loc.currency}</small></div></div>`).join(''):'<div class="empty-state">Ingen priser matcher dette filteret akkurat nå.</div>'});$('#favoriteCount').textContent=s.favorites.length?` ${s.favorites.length}`:''}
async function load(){const token=++loadToken,l=getState().location;priceMeta={source:'loading'};priceData={cokeZero:[],pepsiMax:[]};render();const r=await getPrices(l);if(token!==loadToken)return;priceData=r.data;priceMeta=r;render()}$('#locationButton').onclick=()=>{const m=$('#locationMenu'),o=!m.classList.contains('open');m.classList.toggle('open');$('#locationButton').setAttribute('aria-expanded',o)};$('#locationMenu').onclick=e=>{const b=e.target.closest('[data-location]');if(!b)return;setState({location:b.dataset.location});$('#locationMenu').classList.remove('open');load()};document.addEventListener('click',e=>{if(!e.target.closest('.location-wrap'))$('#locationMenu').classList.remove('open')});
function modal(id,os,cs){const m=$(id);let op;const close=()=>{m.classList.remove('open');document.body.style.overflow='';op?.focus()};$$(os).forEach(b=>b.onclick=()=>{op=b;m.classList.add('open');document.body.style.overflow='hidden';m.querySelector('.continue,input,button')?.focus()});$$(cs).forEach(b=>b.onclick=close);m.onclick=e=>{if(e.target===m)close()};return{m,close}}const about=modal('#aboutModal','[data-about]','[data-close-about]'),tips=modal('#tipsModal','[data-tips]','[data-close-tips]');document.addEventListener('keydown',e=>{if(e.key==='Escape'){about.close();tips.close()}});let n=0;$('#nectarEgg').onclick=()=>{if(++n===5){n=0;toast('Du er åpenbart en av oss.','Jakten fortsetter.')}};$$('.tab').forEach(b=>b.onclick=()=>{$$('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');setState({productFilter:b.dataset.product});render()});$$('.size-chip').forEach(b=>b.onclick=()=>{sizeFilter=b.dataset.size;render()});$('#offerOnly').onchange=e=>{setState({offerOnly:e.target.checked});render()};$('#memberOnly').onchange=e=>{setState({memberOnly:e.target.checked});render()};$('#sortMode').onchange=e=>{setState({sort:e.target.value});render()};$('#priceCards').onclick=e=>{const b=e.target.closest('.heart-btn');if(!b)return;const f=new Set(getState().favorites);f.has(b.dataset.product)?f.delete(b.dataset.product):f.add(b.dataset.product);const a=[...f];localStorage.setItem('sorteGullFavs',JSON.stringify(a));setState({favorites:a});render()};$('#tipsForm').onsubmit=e=>{e.preventDefault();tips.close();e.target.reset();toast('Takk for pristipset!')};load();